lowercase package & module names
rename exceptions that are errors
remove bare excepts
perm vs forgiveness
use namedtuples
Use zip() to iterate through a pair of lists
make decorators link to classes instead of strs
fix class name abbreviations to caps
alphabetize imports
remove wildcard imports
http://docs.quantifiedcode.com/python-anti-patterns


from scap.model.decorators import *

logger

\s{12}'\{([^}]*?)\}([^']*?)': \{([^}]*?)\},?
@attribute(namespace='$1', local_name='$2', $3)

\s{12}'([^']*?)': \{([^}]*?)\},?
@attribute(local_name='$1', $2)

\s{12}\{'xmlns': '([^']+)', 'tag_name': '(.*?)'(, .*?)?\},?
@element(namespace='$1', local_name='$2'$3)

\s{12}\{'tag_name': '(.*?)'(, .*?)?\},?
@element(local_name='$1'$2)

'(enum|default|min|max|list|dict|required)':\s
$1=

'in':\s
into=

'type': '([^']+)'
type=$1

'class': '([^']+)'
cls=$1
